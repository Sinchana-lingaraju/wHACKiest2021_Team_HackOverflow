import 'package:flutter/material.dart';

class Log extends StatelessWidget {
  const Log({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("sleep"));
  }
}
